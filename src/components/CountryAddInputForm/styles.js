import styled from 'styled-components';

export const CountryAddTitle = styled.span`
  font-weight: bold;
`;

export const CountryAddInput = styled.input`
  border-radius: 5px;
  flex: 1;
  padding: 0.3rem;
  margin: 0.3rem;
`;

export const CountryAddButton = styled.button`
    border-radius: 5px;
    flex;
    padding: 0.3rem 1rem;
    margin: 0.3rem;
    cursor: pointer;
`;
